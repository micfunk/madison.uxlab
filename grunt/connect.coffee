module.exports =
  connect:
    options:
      port: 9000,
      hostname: 'localhost'
      keepalive: true
      livereload: true;
    # dist:
    #   options:
    #     base: '<%= yeoman.dist %>'
    # coverage:
    #   options:
    #     port: 9002,
    #     open: true,
    #     base: ['coverage']